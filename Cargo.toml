[workspace]
members = [
    "rust/envmapgen",
    "rust/libc"
]

[profile.release]
lto = true
debug = true
panic = "abort"

[profile.bench]
lto = true
debug = true

[patch.crates-io]
"libc" = { path = "rust/libc" }
"rand" = { path = "rust/rand" }
